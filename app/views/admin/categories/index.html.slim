= link_to 'Nova Categoria', new_admin_category_path
table#dtable.table.table-striped.table-bordered.table-hover.table-checkable.order-column
  thead
    tr
      th Nome
      th Apagar
  tbody
    - @categories.each do |category|
      tr
        td = link_to "#{category.name}", [:admin,category]
        td style="text-align:center"
          = link_to 'X', admin_category_path(category), method: :delete, data: {confirm: 'Deseja apagar essea categoria?'}
