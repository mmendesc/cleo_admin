= link_to 'Novo Cliente', new_admin_client_path
table#dtable.table.table-striped.table-bordered.table-hover.table-checkable.order-column
  thead
    tr
      th Nome
      th E-mail
      th Telefone
      th Celular
      th Aniversário
      th Sexo
      th CEP
      th Apagar
  tbody
    - @clients.each do |client|
      tr
        td = link_to "#{client.name}", [:admin,client]
        td = client.email
        td = client.telephone
        td = client.cellphone
        td = client.birthday
        td = client.sex
        td = client.zipcode
        td style="text-align:center"
          = link_to 'X', admin_client_path(client), method: :delete, data: {confirm: 'Deseja apagar esse cliente?'}
